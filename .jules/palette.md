## 2025-02-23 - Add type='search' to search inputs
**Learning:** Adding `type="search"` to search inputs triggers a native mobile search keyboard and provides built-in browser clear buttons, greatly improving search UX without requiring custom JS or CSS.
**Action:** When adding search inputs to pages in this application, ensure they have `type="search"` applied.
